
import React from 'react';
import { HandHelping as LucideHandHelping } from 'lucide-react';

const HandHelping = (props: React.ComponentProps<typeof LucideHandHelping>) => {
  return <LucideHandHelping {...props} />;
};

export default HandHelping;
